apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: flux-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: flux
spec:
  hosts:
  - "*"
  gateways:
  - flux-gateway
  http:
  - route:
    - destination:
        host: helloworld
